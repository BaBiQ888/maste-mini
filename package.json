{
  "name": "math-mini",
  "private": true,
  "version": "0.1.0",
  "description": "算本 · 3–6 年级数学培训小程序",
  "workspaces": [
    "server"
  ],
  "scripts": {
    "dev": "npm run dev -w server",
    "test": "npm run test -w server",
    "build": "npm run build -w server",
    "typecheck": "npm run typecheck -w server",
    "predeploy": "npm run predeploy -w server",
    "start": "npm run start -w server"
  },
  "engines": {
    "node": ">=20"
  },
  "allowScripts": {
    "better-sqlite3@11.10.0": true,
    "esbuild@0.28.1": true
  }
}
